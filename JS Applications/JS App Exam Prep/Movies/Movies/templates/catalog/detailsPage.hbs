{{>header}}
<div class="container">
    <div class="row bg-light text-dark">
        <h1>Movie title: {{movie.title}}</h1>

        <div class="col-md-8">
            <img class="img-thumbnail" src="{{movie.imageUrl}}" alt="Movie">
        </div>
        <div class="col-md-4 text-center">
            <h3 class="my-3 ">Movie Description</h3>
            <p>{{movie.description}}</p>
            {{#if canEdit}}
            <a class="btn btn-danger" href="#/delete/{{movie._id}}">Delete</a>
            <a class="btn btn-warning" href="#/edit/{{movie._id}}">Edit</a>
            {{else}}
                {{#if canLike}}
                <a class="btn btn-primary" href="#/like/{{movie._id}}">Like</a>
                {{else}}
                <span class="enrolled-span">Liked {{movie.likesCount}}</span>
                {{/if}}
            {{/if}}
        </div>
    </div>
</div>
{{>footer}}